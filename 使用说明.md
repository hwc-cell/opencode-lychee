<a id="top"></a>

# OpenCode-Lychee 使用说明

> [返回项目首页](README.md) · [快速开始](#quick-start) · [TUI 跳转](#tui-navigation) · [主题](#themes) · [微信](#wechat) · [常见问题](#troubleshooting)

本说明面向从源码运行的 OpenCode-Lychee。上游 OpenCode 的插件、供应商和配置文档通常仍然适用；荔枝扩展以本说明为准。

## 快速导航

| 我想做什么                 | 跳转                                |
| -------------------------- | ----------------------------------- |
| 第一次安装并启动           | [快速开始](#quick-start)            |
| 配置模型/API Key           | [连接模型](#providers)              |
| 学会在 TUI 中切页面        | [快捷键与页面跳转](#tui-navigation) |
| 新建、恢复或切换会话       | [会话管理](#sessions)               |
| 更换模型、智能体和思考强度 | [模型与智能体](#models)             |
| 切换荔枝主题               | [主题与亮暗模式](#themes)           |
| 接入微信                   | [微信聊天桥](#wechat)               |
| 使用本地语音输入           | [macOS 语音输入](#voice)            |
| 生成小结或自动记账         | [荔枝扩展](#lychee-features)        |
| 更新源码版本               | [更新与同步](#update)               |
| 排查启动、主题或微信问题   | [常见问题](#troubleshooting)        |

---

<a id="quick-start"></a>

## 1. 快速开始

### 环境要求

- Git
- Bun（推荐使用仓库 `package.json` 声明的版本）
- macOS、Linux 或 Windows；本文的符号链接快捷启动方式适用于 macOS/Linux，Windows 可直接使用 `bun run dev`

### 获取源码

```bash
git clone https://github.com/hwc-cell/opencode-lychee.git
cd opencode-lychee
bun install
bun run dev
```

`bun run dev` 会在当前仓库目录启动 TUI。如果要让 AI 操作另一个项目，推荐安装快捷启动器，再进入目标项目目录启动。

### 安装快捷启动器（macOS/Linux）

在 OpenCode-Lychee 仓库根目录执行：

```bash
mkdir -p ~/.local/bin
ln -sfn "$PWD/packages/opencode/lychee.sh" ~/.local/bin/OpenCode-Lychee
ln -sfn "$PWD/packages/opencode/lychee.sh" ~/.local/bin/lychee
```

确保 `~/.local/bin` 在 `PATH` 中。然后进入希望 AI 操作的项目：

```bash
cd /你的/项目目录
OpenCode-Lychee
```

此时会话工作目录就是启动命令所在的目录。不要从旧仓库副本启动，否则主题和功能可能不是最新版本。

[回到顶部](#top)

---

<a id="providers"></a>

## 2. 连接模型

### 在 TUI 中连接

启动后输入：

```text
/connect
```

选择提供商并按界面提示登录或粘贴 API Key。随后输入 `/models` 选择模型。

### 在终端中连接

```bash
OpenCode-Lychee providers login
OpenCode-Lychee providers list
```

退出某个提供商：

```bash
OpenCode-Lychee providers logout <provider>
```

模型凭据沿用 OpenCode 的本地认证存储。不要把 API Key 写入仓库、README、截图或聊天消息。

[回到顶部](#top)

---

<a id="tui-navigation"></a>

## 3. TUI 快捷键与页面跳转

### 先理解前导键

默认前导键是 `Ctrl+X`。例如 `Ctrl+X` → `T` 的含义是：

1. 先按一次 `Ctrl+X`；
2. 松开；
3. 再按 `T`。

它不是要求三个按键一直同时按住。

### 最常用跳转

| 操作          | 快捷键                                          | 斜杠命令                 |
| ------------- | ----------------------------------------------- | ------------------------ |
| 打开命令面板  | `Ctrl+P`                                        | 在面板中搜索任意功能     |
| 新建会话      | `Ctrl+X` → `N`                                  | `/new` 或 `/clear`       |
| 会话列表      | `Ctrl+X` → `L`                                  | `/sessions`              |
| 模型列表      | `Ctrl+X` → `M`                                  | `/models`                |
| 智能体列表    | `Ctrl+X` → `A`                                  | `/agents`                |
| 主题列表      | `Ctrl+X` → `T`                                  | `/themes`                |
| 状态页面      | `Ctrl+X` → `S`                                  | `/status`                |
| 显示/隐藏侧栏 | `Ctrl+X` → `B`                                  | 可在命令面板搜索“侧栏”   |
| 外部编辑器    | `Ctrl+X` → `E`                                  | 可在命令面板搜索“编辑器” |
| 中断当前任务  | `Esc`                                           | —                        |
| 退出          | 空输入时 `Ctrl+C` / `Ctrl+D`，或 `Ctrl+X` → `Q` | `/exit`、`/quit`、`/q`   |

### 输入框操作

| 操作         | 快捷键                                               |
| ------------ | ---------------------------------------------------- |
| 发送         | `Enter`                                              |
| 换行         | `Shift+Enter`、`Ctrl+Enter`、`Alt+Enter` 或 `Ctrl+J` |
| 清空当前输入 | `Ctrl+C`                                             |
| 撤销输入     | `Cmd+Z` 或 `Ctrl+-`                                  |
| 重做输入     | `Cmd+Shift+Z` 或 `Ctrl+.`                            |

不知道快捷键时，按 `Ctrl+P` 搜索功能最稳妥；按 `/` 可以浏览斜杠命令。

[回到顶部](#top)

---

<a id="sessions"></a>

## 4. 会话管理

- `/new`：回到首页并新建会话；`/clear` 是兼容别名。
- `/sessions`：打开会话列表；方向键移动，`Enter` 打开。
- 会话列表中 `Ctrl+R` 重命名、`Ctrl+D` 删除、`Ctrl+F` 固定/取消固定。
- `Ctrl+X` → `1`…`9`：跳转到快速槽位中的会话。
- `Esc`：中断当前正在运行的模型或工具。
- `Ctrl+X` → `C`：压缩当前长会话上下文。

启动时恢复指定会话：

```bash
OpenCode-Lychee -s <session-id>
OpenCode-Lychee --continue
```

[回到顶部](#top)

---

<a id="models"></a>

## 5. 模型、智能体与思考强度

| 功能         | 操作                        |
| ------------ | --------------------------- |
| 选择模型     | `/models` 或 `Ctrl+X` → `M` |
| 收藏模型     | 在模型列表中按 `Ctrl+F`     |
| 切换最近模型 | `F2`；反向为 `Shift+F2`     |
| 选择智能体   | `/agents` 或 `Ctrl+X` → `A` |
| 循环智能体   | `Tab`；反向为 `Shift+Tab`   |
| 选择思考强度 | `/variants`                 |
| 循环思考强度 | `Ctrl+T`                    |
| 查看 MCP     | `/mcps`                     |

如果模型列表为空，先运行 `/connect` 或 `OpenCode-Lychee providers login`。

[回到顶部](#top)

---

<a id="themes"></a>

## 6. 主题与亮暗模式

输入 `/themes`，或按 `Ctrl+X` → `T`：

1. 输入主题名称进行筛选；
2. 用方向键预览；
3. 按 `Enter` 确认；
4. 按 `Esc` 取消并恢复原主题。

荔枝内置主题包括：

| 主题 ID                 | 中文名称   | 风格                   |
| ----------------------- | ---------- | ---------------------- |
| `lychee`                | 荔枝经典   | 暖红色品牌主题         |
| `lychee-clear-sky`      | 晴空玻璃   | 接近白色的浅蓝         |
| `lychee-soda`           | 荔枝汽水   | 浅青蓝与荔枝红         |
| `lychee-celadon`        | 荔枝青瓷   | 青瓷绿与朱红           |
| `lychee-china-red`      | 荔枝山河红 | 中国红、宫墙金与宣纸色 |
| `lychee-osmanthus-moon` | 荔枝桂月   | 中秋月色与桂花金       |

详细颜色与设计原则见[荔枝主题色板记录](docs/lychee-theme-palettes.md)。

切换亮色/暗色模式：按 `Ctrl+P`，搜索“浅色模式”“深色模式”或“主题模式”。主题支持跟随终端，也可以在命令面板中锁定模式。

更新源码后新增主题没有出现时，需要完全退出并重新打开 TUI；已经运行的进程不会热加载内置主题。

[回到顶部](#top)

---

<a id="wechat"></a>

## 7. 微信聊天桥

### 第一次连接

```bash
OpenCode-Lychee weixin login
OpenCode-Lychee weixin configure
OpenCode-Lychee weixin autostart   # macOS 后台常驻
```

`login` 会显示二维码；首次成功后会引导选择默认模型。非 macOS 系统或临时调试可运行：

```bash
OpenCode-Lychee weixin run
```

### 管理命令

```bash
OpenCode-Lychee weixin status
OpenCode-Lychee weixin configure
OpenCode-Lychee weixin autostop
OpenCode-Lychee weixin logout
```

### 微信聊天内指令

| 指令                       | 作用                                           |
| -------------------------- | ---------------------------------------------- |
| `/help`                    | 查看微信内帮助；`/halp` 仍兼容                 |
| `/model`                   | 查看模型列表                                   |
| `/model <名称> [强度]`     | 切换当前聊天的模型和思考强度                   |
| `/new`                     | 立即创建新 AI 会话                             |
| `/clear`                   | 清除当前会话，下条消息重新创建                 |
| `/stop`                    | 停止当前任务                                   |
| `/status`                  | 查看模型、工作目录和会话状态                   |
| `/where`                   | 查看 AI 工作目录                               |
| `/where <目录>`            | 切换工作目录并清除旧会话；仅扫码登录账号可操作 |
| `/autostart` / `/autostop` | 管理 macOS 后台常驻；仅扫码登录账号可操作      |

### 图片、文件与语音

- 支持文字、微信语音转写、图片、文件和视频附件。
- 默认单附件上限 10MB，单条消息附件总计 20MB。
- 可通过 `LYCHEE_MEDIA_MAX_BYTES` 和 `LYCHEE_MEDIA_TOTAL_MAX_BYTES` 调整字节上限。
- 原始音频或视频能否被理解取决于当前模型是否支持相应格式。
- 媒体只从可信微信 CDN 下载，在内存中解密，不写入临时文件。

后台日志默认位于：

```text
~/.local/state/opencode/weixin.log
```

[回到顶部](#top)

---

<a id="voice"></a>

## 8. macOS 语音输入

安装本地 Whisper 和按键监听器：

```bash
OpenCode-Lychee voice install
OpenCode-Lychee voice authorize
```

授权成功后，按住左 Command 说话，松开后会把转写结果填入 TUI 输入框。

其他管理命令：

```bash
OpenCode-Lychee voice start
OpenCode-Lychee voice stop
OpenCode-Lychee voice status
OpenCode-Lychee voice test
```

可用模型：`tiny`、`base`、`small`，例如：

```bash
OpenCode-Lychee voice install --model small
```

[回到顶部](#top)

---

<a id="lychee-features"></a>

## 9. 荔枝小结与自动记账

### 会话小结

输入 `/summary` 开启或关闭。旧命令 `/lycheexj` 仍然兼容。开启后，退出 TUI 时会使用当前模型生成中文小结并保存到：

```text
当前项目/.opencode/reports/
```

### 荔枝记账

1. 在荔枝记账中生成 API Key；
2. 复制 Key 到系统剪贴板；
3. 在 TUI 输入 `/ledger-key`，程序会读取剪贴板并校验保存；
4. 输入 `/autolychee` 开启或关闭自动记账。

API Key 和微信登录凭据属于敏感数据，不要写入项目文件或公开日志。

[回到顶部](#top)

---

<a id="cli"></a>

## 10. 常用终端命令

```bash
OpenCode-Lychee --help
OpenCode-Lychee providers list
OpenCode-Lychee models
OpenCode-Lychee stats
OpenCode-Lychee session --help
OpenCode-Lychee plugin --help
OpenCode-Lychee mcp --help
```

直接让 AI 执行一次任务而不进入 TUI：

```bash
OpenCode-Lychee run "检查当前项目并总结问题"
```

[回到顶部](#top)

---

<a id="update"></a>

## 11. 更新与同步

本项目是源码 fork，快捷启动器直接链接到仓库。推荐使用 Git 更新：

```bash
cd /path/to/opencode-lychee
git status
git pull --ff-only origin branding-lychee
bun install
```

如果仓库被移动，需要重新创建 `~/.local/bin/OpenCode-Lychee` 链接。荔枝启动器默认关闭上游自动更新，避免官方 OpenCode 包覆盖 fork 功能。

[回到顶部](#top)

---

<a id="troubleshooting"></a>

## 12. 常见问题

### 找不到 `OpenCode-Lychee` 命令

确认 `~/.local/bin` 已加入 `PATH`，并检查启动器：

```bash
ls -l ~/.local/bin/OpenCode-Lychee
```

### 新主题没有出现

1. 完全退出旧 TUI；
2. 确认启动器指向当前维护的仓库，而不是另一个旧副本；
3. 重新运行 `OpenCode-Lychee`；
4. 输入 `/themes` 搜索 `lychee`。

### 模型列表为空

运行 `/connect` 或 `OpenCode-Lychee providers login`，然后重新打开 `/models`。

### 微信没有回复

```bash
OpenCode-Lychee weixin status
tail -n 100 ~/.local/state/opencode/weixin.log
```

如果状态显示登录失效，重新运行 `OpenCode-Lychee weixin login`。

### 微信附件失败

- 检查附件大小是否超过限制；
- 确认模型支持对应图片、PDF、音频或视频格式；
- 查看 `weixin.log` 中的 CDN 下载或 AES 解密错误；
- 不要把媒体限制设得过大，data URI 会增加本地请求体大小。

### 需要查看所有可用操作

- TUI：按 `Ctrl+P`；
- TUI 斜杠命令：输入 `/`；
- CLI：运行 `OpenCode-Lychee --help`；
- 微信：发送 `/help`。

[回到顶部](#top)

---

## 数据位置

| 数据                     | 默认位置                              |
| ------------------------ | ------------------------------------- |
| 微信凭据、会话映射和状态 | `~/.local/state/opencode/weixin.json` |
| 微信后台日志             | `~/.local/state/opencode/weixin.log`  |
| 模型认证信息             | OpenCode 本地认证存储                 |
| 语音模型                 | `~/.local/share/opencode/whisper/`    |
| 荔枝小结                 | `<项目>/.opencode/reports/`           |

分享日志或截图前，请先移除 API Key、登录 Token、微信凭据、目录中的隐私信息和财务数据。

[返回项目首页](README.md) · [回到顶部](#top)
